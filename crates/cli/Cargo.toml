[package]
name = "http-client-cli"
version = "0.1.0"
edition = "2021"
description = "Run .http requests from the terminal, CI, and editor tasks"
license = "MIT"

[[bin]]
name = "http-client"
path = "src/main.rs"

[dependencies]
serde_json = "1"
http-client-core = { path = "../core" }
