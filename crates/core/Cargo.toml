[package]
name = "http-client-core"
version = "0.1.0"
edition = "2021"
description = "Core parser, variable resolution, and execution model for HTTP request files"
license = "MIT"

[dependencies]
serde_json = "1"
